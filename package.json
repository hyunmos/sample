{
  "name": "root",
  "private": true,
  "scripts": {
    "install": "lerna exec -- yarn",
    "delete": "lerna exec -- rm -rf ./node_modules"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
